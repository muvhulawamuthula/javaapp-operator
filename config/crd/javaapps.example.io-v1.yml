# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "javaapps.example.io"
spec:
  group: "example.io"
  names:
    kind: "JavaApp"
    plural: "javaapps"
    shortNames:
    - "japp"
    singular: "javaapp"
  scope: "Namespaced"
  versions:
  - additionalPrinterColumns:
    - description: "Application version currently running."
      jsonPath: ".status.currentVersion"
      name: "Version"
      priority: 0
      type: "string"
    - description: "Pending | Running | Upgrading | Degraded | Terminating"
      jsonPath: ".status.phase"
      name: "Phase"
      priority: 0
      type: "string"
    - description: "Number of ready pods."
      jsonPath: ".status.readyReplicas"
      name: "Ready"
      priority: 0
      type: "integer"
    name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              env:
                additionalProperties:
                  type: "string"
                description: "Extra environment variables injected into every pod."
                type: "object"
              hpaManaged:
                description: "When true the operator will NOT manage replica count\
                  \ (leave it to an HPA)."
                type: "boolean"
              image:
                description: "OCI image reference, e.g. eclipse-temurin:21-jre"
                type: "string"
              replicas:
                default: 1
                description: "Number of replicas. Defaults to 1."
                minimum: 0.0
                type: "integer"
              resources:
                description: "CPU and memory requests/limits for each pod."
                properties:
                  limitCpu:
                    description: "CPU limit. Defaults to 1."
                    type: "string"
                  limitMemory:
                    description: "Memory limit. Defaults to 512Mi."
                    type: "string"
                  requestCpu:
                    description: "CPU request, e.g. 500m. Defaults to 250m."
                    type: "string"
                  requestMemory:
                    description: "Memory request, e.g. 256Mi. Defaults to 256Mi."
                    type: "string"
                type: "object"
              storage:
                description: "PersistentVolumeClaim template for the StatefulSet."
                properties:
                  accessMode:
                    default: "ReadWriteOnce"
                    description: "PVC access mode. Defaults to ReadWriteOnce."
                    type: "string"
                  claimName:
                    default: "data"
                    description: "Volume claim name. Defaults to data."
                    type: "string"
                  mountPath:
                    default: "/data"
                    description: "Mount path inside the container. Defaults to /data."
                    type: "string"
                  size:
                    description: "Storage size, e.g. 10Gi"
                    pattern: "^[0-9]+(\\.[0-9]+)?(Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|k)?$"
                    type: "string"
                  storageClassName:
                    description: "StorageClass name. Omit to use the cluster default\
                      \ StorageClass."
                    type: "string"
                required:
                - "size"
                type: "object"
              version:
                description: "Logical application version, e.g. 1.2.0. Changing it\
                  \ triggers a managed rolling upgrade."
                type: "string"
            required:
            - "image"
            - "storage"
            type: "object"
          status:
            properties:
              conditions:
                description: "Kubernetes-style status conditions."
                items:
                  properties:
                    lastTransitionTime:
                      type: "string"
                    message:
                      type: "string"
                    observedGeneration:
                      type: "integer"
                    reason:
                      type: "string"
                    status:
                      type: "string"
                    type:
                      type: "string"
                  type: "object"
                type: "array"
              currentVersion:
                description: "Application version currently running."
                type: "string"
              lastReconcileTime:
                description: "ISO-8601 timestamp of the last reconciliation."
                type: "string"
              phase:
                description: "Pending | Running | Upgrading | Degraded | Terminating"
                type: "string"
              readyReplicas:
                description: "Number of ready pods."
                type: "integer"
              updatedReplicas:
                description: "Number of updated pods."
                type: "integer"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
